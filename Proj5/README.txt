Run “npm install” to install all dependancies

Create a new folder here called "database"

Run "mongod --dbpath=database" to set up database
Run the database-initializer.js file for testing
Run the server: "node server.js" 

All I did were sending friend requests, but even thats pretty buggy too. there's no card stuff.